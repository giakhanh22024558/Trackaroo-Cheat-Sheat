# Gap & Clarification Register

> **Purpose:** Log of **spec ambiguities, gaps, and clarification questions** discovered while analysing the 19 spec docs / building the backlog. Each entry asks one specific question of the Project Director (or relevant authority) and records a working assumption so build work can continue.
>
> Format modelled on the MasterMind [`document/analysis`](../MasterMind/models/model_001/document/analysis/) pattern + the **Clarification Register** convention used in the Slitigenz proposal §11 (CLR-SLZ-NNN).

## Conventions

| Field | Notes |
|---|---|
| **ID** | `CLR-TRK-NNN` — Clarification · Trackaroo · sequential (distinct from Slitigenz's `CLR-SLZ-NNN`) |
| **Status** | `Open` (awaiting PD) · `Assumed` (working assumption applied, build proceeds) · `Resolved` (PD confirmed) · `Superseded` (replaced by another clarification or spec amendment) |
| **Impact** | What breaks / is at risk if the assumption is wrong |
| **Working assumption** | What the team builds against until clarified — never block; build-and-adjust per WFD-5126 `Build-against-assumptions` principle |
| **Source** | Spec doc + section the question arises from |
| **Logged / Resolved** | dates (YYYY-MM-DD) |
| **Topic** | Free-text label grouping related CLRs (e.g. *"Safe Anchor Point"*, *"SOS Flow"*, *"Data Persistence"*). Reuse existing labels when possible — keeps the register filterable/sortable |

> **Escalation rule:** every `Open` entry should have a logged date + an owner. Anything `Open > 2 weeks` and gating Sprint 0 deliverable → escalate to PD via the gate-evidence channel.

---

## Register

| ID | Topic | Source | Question | Working assumption | Impact | Status | Logged | Client answer | Resolved |
|---|---|---|---|---|---|---|---|---|---|
| **CLR-TRK-001** | Safe Anchor Point | TAA §8.1 / §8.2 · FSD §5.2.1 · TQP §5.5.2 | **Cap appropriateness — commercial vs survival** *(answer this first — gates downstream design)*. Does **Free = 3 anchors** truly satisfy TAA §8.2 *"genuinely survival-capable, not crippled preview"*? Tensions: (a) Anchor is local-only, no infra cost → cap is purely commercial design, not technical; (b) spec self-describes anchors as *"emergency rally points"* (safety-adjacent) → conflicts with §8.1 *"don't monetise survival"*; (c) survival-grade multi-day use (home + trailhead + parking + water + campsite + rally) easily exceeds 3 → *"crippled preview"* risk; (d) industry norm (Google Maps starred, Apple favorites, Gaia) = unlimited local markers. Should caps revise (e.g. Free=10 / Plus=50 / Pro=unlimited) OR Anchor reclassify to Survival Core (uncapped)? | Build to spec as-is (3/20/50) — proceed unchanged. Surface as UX/commercial risk to PD; do not unilaterally re-cap. | **Medium.** Unchanged: marketing risk (perceived crippled preview vs industry norm); safety-adjacent risk if Free user can't save adequate rally points. Revised: commercial revenue model may need re-tune. **Gates CLR-TRK-002:** if Anchor reclassifies to Survival Core (uncapped), the entire downgrade-handling question disappears. | **Open** | 2026-05-28 | *(awaiting PD)* | — |
| **CLR-TRK-002** | Safe Anchor Point | FSD §5.2.4 · OCS §6.6 + AOD §51 / PSB §21 / PRD §182 / UXS §75 / TAA §8.1 / TQP §5.5.2 | **Downgrade handling — detection & UX response** *(only relevant if CLR-TRK-001 confirms tier cap)*. §5.2.4 mandates the 8-step downgrade flow + wireframe but leaves two gaps: **(How) Detection** — what triggers the flow when downgrade source is IAP auto-expiry (Apple/Google not renewed → tier drops while anchors > new cap)? On app launch? Real-time? Other? **(What) Handling** — per 4 authority mandates (E&I Layer must NEVER block Survival Core), the selection screen cannot hard-block app use mid offline-nav, yet §5.2.4 implies a PERMANENT-delete modal. Forcing irreversible deletion under stress (anchor possibly being used as rally point) is dangerous. What is the required UX flow? | **(How) Detection:** App checks IAP state **on app launch** (online refresh if connected, else last-known cache). If tier↓ + count > new cap → enters flow. **(What) Handling:** Over-cap anchors enter **read-only mode** during active nav (visible & navigable, no new creation). Selection screen **deferred** to next safe transition (end of nav session OR app online in safe state). Survival Core never blocked. Anchors never silently deleted (per §5.2.1). Tier-cap = graceful degradation, not hard gate. | **HIGH (safety).** If detection ambiguous → silent anchor loss on offline expiry (violates §5.2.1 + UXS transparency). If handling blocks Survival Core mid-nav → violates 4 authority "never block" mandates + TAA §8.1 *"never monetise survival"* + endangers user under stress (forced permanent delete of rally points) + breaches UXS calm posture. | **Open** | 2026-05-28 | *(awaiting PD)* | — |
| **CLR-TRK-003** | Safe Anchor Point | FSD §5.2.5 (Edit + Delete rules in same paragraph) · BTF §5.2 (BackTrack append-only precedent, for contrast) | **§5.2.5 internal contradiction — coord-immutability rationale vs hard-delete rule.** §5.2.5 prohibits Edit coord with rationale *"prevents silent overwrite of safety-critical coordinate"* (implies original record must be preserved), but permits Delete as *"immediate and permanent · no trash or undo model"* (purges original instantly). The two rules cannot both be justified by preservation: Delete + Create New achieves the same coordinate overwrite as Edit, with the original equally destroyed — only friction differs. What is the real intent? **(a)** **Cognitive deliberation friction + anti-mistap** (2-step destroy+create is slower than 1-step edit, forces explicit "I am destroying this rally point" confirmation) → if so, update wording: drop *"silent overwrite"*, state friction intent honestly. **(b)** **Forensic preservation** of original record → if so, Delete should be tombstone with retention window (e.g. 30-day soft-delete), not hard-purge. **(c)** **BackTrack-style immutability** (record sacred) → if so, Delete should also be prohibited (consistent with breadcrumb 14-prohibited-mutations rule). PD must pick one direction so spec is self-consistent. | Build to spec literally: no edit, immediate hard-delete with single confirmation. **Do not infer** forensic intent — spec wording does not support it. Treat coord-immutability rule purely as a cognitive-friction guard until PD clarifies. | **Medium.** Sai rationale → vendor may reintroduce Edit (*"rationale weak, friction-equivalent → just add edit"*) → breaks cognitive-friction guard if that is the real intent. Or vendor may convert Delete to tombstone (*"to fix forensic gap"*) → adds storage + restore UX not in scope. Spec self-contradiction = each vendor interprets differently → inconsistent implementations across Phase 1/2. | **Open** | 2026-05-31 | *(awaiting PD)* | — |
| **CLR-TRK-004** | Point Condition Report | OSM §10.3 (confirmation threshold + supersession) · CDG §5.5 (PCR schema + prohibited fields) | **Proximity threshold for "same point" undefined + count mechanism contradiction.** OSM §10.3 uses "same point/location" semantics for both supersession (paths 1+2) and confirmation transition (1st amber → 2nd red), but never defines proximity tolerance. Example: User A at lat1/lng1, User B at lat2/lng2 with ~100m drift (typical handheld GPS error in canopy/canyon) — same point or distinct? Compounded by internal contradiction: CDG §5.5 explicitly **PROHIBITS** field `confirmation_count` as *"density metric"*, yet OSM §10.3 mandates count semantics (*"2nd submission = confirmed"*). How is "2nd" computed without a counter? Cluster query on read? Define both (1) proximity radius (e.g., 25m? 50m? archetype-dependent?) and (2) storage mechanism (query-time spatial aggregation? indexed proximity hash?). | Build with **25m radius proximity clustering computed at read time** (no `confirmation_count` field — count derived on demand via spatial query over `coordinates_lat/lng` within radius + distinct `reporter_archetype`). Aligns with typical handheld GPS accuracy + avoids CDG §5.5 schema prohibition. | **HIGH.** Without defined threshold: (a) 2 legitimate independent reports of same fallen tree at 100m apart never confirm → stay amber forever, weakens visual escalation; (b) each vendor implements own radius → cross-device + cross-implementation inconsistency. Without storage clarification: vendor may add prohibited `confirmation_count` field → CDG §5.5 RG violation. Gates correct behaviour of confirmation + supersession logic. **Gates CLR-TRK-010** — visual clustering only meaningful once data-side "same point" threshold is locked. | **Open** | 2026-05-31 | *(awaiting PD)* | — |
| **CLR-TRK-005** | Point Condition Report | WFD PCR-WF-03 · OSM §10.5 · contrast: FSD §5.2.7 (Anchor — explicit 3 methods) | **PCR point-specification UX methods not enumerated.** WFD PCR-WF-03 + OSM §10.5 both require user to *"confirm point location on map"* without enumerating methods. By contrast, Anchor §5.2.7 explicitly lists 3 methods (current GPS / map tap / manual lat-lng entry). Which methods are permitted for PCR? Map tap only? GPS-default with editable position? Both? Manual coord entry? Vendor risk: (a) GPS-only → user can't report a hazard 50m ahead while still moving; (b) map-tap only → user must precisely locate themselves on tile under stress; (c) manual coord → unlikely useful but spec doesn't exclude. Inconsistency with Anchor module on same nature (user-placed point markers). | Build to **Anchor parity** — support all 3 methods (current GPS default with drag-on-map to adjust, map tap, manual lat-lng entry). Default = current GPS to minimise friction during navigation; user can adjust before confirm. | **Medium.** UX inconsistency between modules with same nature → vendor diverges, re-work + Discovery review concerns. If vendor restricts to GPS-only → user cannot report hazard seen ahead → underuse of PCR system. | **Open** | 2026-05-31 | *(awaiting PD)* | — |
| **CLR-TRK-006** | Point Condition Report | OSM §10.3 (token distinctness rule) · CDG §5.5 (`reporter_archetype` schema) | **Archetype token scope undefined + Sybil resistance absent.** OSM §10.3 requires *"different anonymised archetype token"* for confirmation count, but token derivation/scope is nowhere defined. Possible scopes: (a) per-device → 1 user with 2 phones = 2 distinct tokens, trivially fakes confirmation; (b) per-account → multi-account, same problem with more friction; (c) per-archetype-per-account → user switches archetype to gen new token; (d) cryptographically derived per-install-per-account → most defensible. No rate-limit / proximity-time check specified — 2 submissions from 2 tokens within 30s at <5m apart should plausibly flag as suspicious but spec is silent. Define: (1) token derivation rule; (2) anti-Sybil heuristics (rate-limit, proximity-time, archetype-flip detection). | Token = HMAC(account_id, install_id) → stable per install, distinct across devices/accounts. **Flag (not block) confirmation if 2nd submission arrives <60s after 1st from a different token at <10m distance** — surface to OCS review queue. | **HIGH.** Without token discipline + anti-Sybil checks, "2 independent reporters = confirmed" rule is decorative. Single user or small group can confirm fake PCRs — especially safety-critical PCR-CLO (fake closure) / PCR-WTR (fake water depth). Resolution-side abuse (CLR-TRK-009) compounds the risk. | **Open** | 2026-05-31 | *(awaiting PD)* | — |
| **CLR-TRK-007** | Point Condition Report | OSM §10.5 (no pre-moderation specified) · OCS §6.2 (Suppression + Account Suspension) · CDG §5.5 + §10 (anonymity rule — only `reporter_archetype` exposed) | **End-to-end abuse control — pre-pub window + anonymity-vs-suspension contradiction.** Two coupled gaps: **(a) Pre-publication moderation window** — spec specifies no moderation gate; PCRs auto-publish on sync. Malicious PCR-CLO ("track closed") or PCR-WTR ("dangerous depth") misleads users in the window between publish and Ops suppression. No response SLA. **(b) Anonymity vs accountability contradiction** — CDG §5.5/§10 mandate PCR strictly anonymised (only `reporter_archetype`, no PII). Yet OCS §6.2 includes Account Flag + Account Suspension, which presuppose a server-side `reporter_archetype → account_id` link. This link is functionally required for abuse enforcement but is nowhere documented. Need: (1) confirm backend link exists; (2) who can reverse-lookup + under what authority; (3) Ops suppression SLA target (e.g., suspected malicious PCR-CLO/WTR auto-flagged + suppressed within Xh); (4) escalation flow to legal disclosure (e.g., PCR fake causing injury). | Backend stores `archetype_token → account_id` mapping accessible only to PD via Ops audit reverse-lookup tool (Phase 2 build, not Phase 1 scope). Phase 1: rely on community supersession + manual Ops weekly sweep; no SLA. Vendor implements **no client-visible** reverse-lookup capability. | **HIGH (safety).** PCR-CLO / PCR-WTR fakes are life-safety vectors. Lack of pre-moderation + lack of articulated enforcement → vendor builds weakest interpretation. Lack of documented backend link → vendor may design storage without it → OCS Account Suspension is non-functional → entire abuse pipeline broken. | **Open** | 2026-05-31 | *(awaiting PD)* | — |
| **CLR-TRK-008** | Point Condition Report | OSM §10.3 (*"no automated expiry"*) · OSM §10.4 (>90d muted + *"Condition age unknown"*) · WFD PCR-WF-05 | **Stale-but-live PCR — UX guidance missing.** Per OSM §10.3 PCRs have no TTL expiry; only muted visual + "Condition age unknown" label after 90d unconfirmed. UX risk: encountering an amber PCR 6+ months old creates ambiguity (still relevant? cleared but no one re-reported? abandoned trail with no traffic to confirm-clear?). Spec is intentional about no auto-expiry (per supersession-only model) but provides no UX guidance for: (a) detail card content for muted PCRs (should it surface *"last activity N months ago"*?); (b) optional in-app nudge to encourage re-confirm when user passes near muted PCR (*"You're near a 4-month-old report — confirm if still valid?"*); (c) graceful degradation in low-traffic regions where 90d muting may apply to most PCRs (signal value of muted vs unmuted collapses). | Build mute treatment per OSM §10.4 literally; **no nudge or re-confirm prompt** in Phase 1 (out-of-scope addition). Detail card surfaces submission date + *"Condition age unknown"* label only. Surface user-research request for Phase 2 nudge design. | **Low-Medium.** UX clarity, not safety-blocker. Risk under low-traffic conditions: most PCRs muted → user habituates → loses signal value of muted vs unmuted distinction → ignores legitimate stale-but-valid warnings. | **Open** | 2026-05-31 | *(awaiting PD)* | — |
| **CLR-TRK-009** | Point Condition Report | OSM §10.3 (3 supersession paths, asymmetric to §10.3 confirmation rule) | **Resolve symmetry — confirm needs 2 reporters, resolve needs only 1.** Confirmation (amber→red visual escalation) requires 2 independent reporters (OSM §10.3). But RESOLUTION (active→resolved, removed from active map) is asymmetric — only 1 reporter suffices via any of 3 paths: (1) ANY subsequent user reports cleared → resolved; (2) any subsequent user submits new PCR same point → supersedes; (3) original reporter self-resolves → instant. Scenarios to clarify: **(a)** After confirmed PCR (red ring, A+B reported), can User C — who never saw the original — single-handedly resolve it? Per §10.3 path 1 → YES. Intended? **(b)** Self-resolution by A instant, no confirmation needed? Per §10.3 path 3 → YES. Intended? **(c)** Should resolve require 2 independent "cleared" reports for symmetry — especially safety-critical PCR-CLO + PCR-WTR? Resolution-side abuse is **strictly more dangerous** than confirm-side: removes existing warning (user navigates into actual closure unaware) vs adds false marker. | Build to spec literally — 1 cleared-report resolves (including from never-saw-original user); self-resolve by A instant. Surface category-specific asymmetry concern (PCR-CLO / PCR-WTR resolve should plausibly require 2-reporter symmetry) to PD as design risk; do not unilaterally add threshold. | **HIGH (safety).** Resolution-side abuse strictly worse than confirm-side: 1 malicious actor wipes real PCR-CLO → users navigate into actual emergency closure. Asymmetry between confirm (2-reporter) + resolve (1-reporter) thresholds lacks design rationale. Compounds CLR-TRK-006 Sybil gap: 1 fake token can resolve a real warning. | **Open** | 2026-05-31 | *(awaiting PD)* | — |
| **CLR-TRK-010** | Point Condition Report | CR04 Q17 (*"no defined maximum PCR count per viewport — vendor must propose clustering"*) · OSM §10.4 (visual treatment) · OSM §5A.5 (PCR markers topmost z-index; 44×44pt min / 60×60pt recommended touch targets) · MAS-5126 Part B (PCR icon design authority) | **PCR clustering / viewport density rule absent.** CR04 Q17 confirms there is NO defined maximum PCR count per viewport; vendor required to propose clustering. OSM §10.4 + §5A.5 set marker constraints (topmost z-index, 60×60pt recommended touch) but say nothing about overlap handling. High-density scenarios (e.g., flood event with many PCR-WTR along a creek, major closure with stacked PCR-CLO/INF/HAZ) → markers overlap, obscure track-line difficulty/shield state (already prohibited by LIR-04 + RG-07), become un-tappable. Spec silent on: (a) clustering trigger threshold (e.g., cluster when ≥2 markers overlap within 60×60pt bbox at current zoom); (b) cluster icon design (category-mixed glyph? count badge? mixed amber/red ring?); (c) tap-to-expand UX (vertical list of N stacked PCRs? carousel? card stack?); (d) interaction with PCR-WF-01 wireframe + PCR-WF-02 detail tap; (e) zoom-level behaviour (does pinch-zoom break clusters apart at threshold zoom?). | Build **cluster-on-overlap** at all zoom levels — when ≥2 PCR markers within 60×60pt bbox, merge into cluster icon (category-mixed glyph + count badge, ring colour = highest severity present). Tap expands to vertical list view; each row tappable to its detail card per PCR-WF-02. Maintain topmost z-index per §5A.5. | **Medium-High.** Without resolution: (a) high-density PCR areas become tap-impossible — user cannot access safety info on real warnings; (b) overlapping markers obscure difficulty/shield → LIR-04 / RG-07 governance breach + release-gate failure; (c) each vendor implements own clustering → inconsistent UX, Discovery review concerns. **Depends on CLR-TRK-004** — pixel cluster threshold must be calibrated with data-side proximity threshold (else visual cluster of "2 reports" can lie about underlying confirmed/unconfirmed state). | **Open** | 2026-05-31 | *(awaiting PD)* | — |

---

## How to use

- **Log a new question:** add a row with next `CLR-TRK-NNN`, fill all fields. Status starts `Open`.
- **Apply working assumption:** if the team must proceed before PD answer, document the assumption clearly so the impact of being wrong is known + recoverable. Per WFD-5126 build-against-assumptions: ship against the assumption; adjust on the next iteration when PD clarifies.
- **Record the answer:** when PD/client responds, paste the verbatim answer into **Client answer** (keep their wording — don't paraphrase). If the answer matches the working assumption: set Status `Resolved` + fill Resolved date. If it differs: update assumption to match the answer, set Status `Resolved`, log the implementation delta wherever the change applies. Keep the row either way (audit trail).
- **Link from planning:** if a clarification gates a Sprint 0 task or feature, reference its `CLR-TRK-NNN` in the relevant `docs/planning.md` row.

## Related registers (not duplicated here)

| Register | Where | What it covers |
|---|---|---|
| **Slitigenz vendor clarifications** (`CLR-SLZ-001 → 006`) | `research/spec-docs/Slitigenz-Proposal-RFT5026.md` §11 | Vendor's working assumptions submitted with proposal (Vietnam validation lab, device matrix, WCAG audit method, hazard-feed format, OCS Firebase project, SOS copy delivery format) |
| **Open mismatches (low-impact)** | `research/spec-docs/README.md` → "Open mismatches" | Documentary inconsistencies between spec docs (7-vs-8 tracks, VEG-5026 vs VGD-5126, FRM-5226 vs FRM-5126) — flagged for PD but not gating |
| **Change Requests (post-SRS)** | `docs/gap-analysis.md` | 17-col Gap + Impact analysis for client CRs once SRS exists |
